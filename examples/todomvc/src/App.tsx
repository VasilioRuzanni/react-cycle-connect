import React from 'react';
import TodosScreen from './todos/TodosScreen';

export function App() {
  // Here's just one feature currently for demo purposes.
  // Instead, there could be multiple features' screens,
  // rendered by router, etc.
  return <TodosScreen />;
}

export default App;
